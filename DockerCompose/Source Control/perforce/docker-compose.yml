# Example docker-compose.yml snippet for p4d
version: '3'
services:
  perforce:
    image: ambakshi/perforce-server
    hostname: perforce
    ports:
      - "1666:1666"
    volumes:
      - p4depot:/data
    env_file:
      - ./envfile

volumes:
  p4depot:   